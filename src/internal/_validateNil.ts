import _isUndefined from './_isUndefined';
import _isNull from './_isNull';

const _validateNil = (specs, value) => {
  // if (_isNull(value)) return allowNull ? 'results' : 'error';
  // if (_isUndefined(value)) return allowUndefined ? 'results' : 'error';
  // if (isNaN(value)) return 'error';
};

export default _validateNil;
