import * as R from 'ramda';

/*---------------------------------------------------------------
  Each grouping can only contain max 4
  Only max 8 groups
  Empty values allowed replaced with ::
  Multiple empty values can be replaced with ::
  ::1
  
  Allow characters [0-9abcdef:]
  ---------------------------------------------------------------*/
